module PythonTypeLanguage
{
   type = AtomicType(identifier name)
        | TypeVariable(identifier name)
        | FunctionType(type* anon_args, type? args_type, type? kwargs_typ)
        | TypeApplication(type fn, type* args)
        | Union(type* types)

   constraint = SubtypeOf(type, type)
}
